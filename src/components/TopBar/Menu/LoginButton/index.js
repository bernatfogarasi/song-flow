import styled from "styled-components";

const Wrapper = styled.div`
  width: 400px;
  height: 200px;
  position: absolute;
  top: 0px;
  right: 0px;
`;

const LoginButton = () => {
  return <Wrapper>LoginButton</Wrapper>;
};

export default LoginButton;
