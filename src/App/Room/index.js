import styled from "styled-components";
import Player from "./Player";

const Wrapper = styled.div``;

const Room = () => {
  return (
    <Wrapper>
      <Player />
    </Wrapper>
  );
};

export default Room;
